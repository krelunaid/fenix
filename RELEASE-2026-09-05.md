# Verified release scope — 5 September 2026

This release integrates the independently verified source changes from local
`98a9b7b9d1eabb3481fe9e352becbd6f576bb4a0` onto real upstream main
`f21d40dd0931b92da758714849fd8409777e98bd`.

The user explicitly selected publication of this verified version now. Later
Grok-only work (`783420d` / `956ded4`) has not been transferred and is excluded;
it remains available in that conversation for a subsequent reviewed integration.
No original Grok commit ancestry is fabricated by this source integration.

## Included

- Recovered and previously reviewed generated-app typography, palette and icon
  dependencies through Grok's d70 source, followed by Codex's verified fixes.
- Opt-in native-inspired generated-app styling, literal/atomic edits, bounded
  worker requests, truthful worker failure reporting, stream artifact limits,
  and SHA-bound composed-worker build plans preserving the original palette.
- Regression tests and protocol documents describing remaining limitations.

## Preserved and excluded

- Fenix Studio, deployment configuration, credentials, dependencies and upstream
  history are preserved. This is not a wholesale replacement with a snapshot.
- Previously generated screenshot/JSON fixture output and a reconstructed
  Gradle binary are not imported as new release evidence. Tests may regenerate
  fixtures in disposable verification clones.
- The normal non-iOS stream-first path does not yet use the composed-worker
  patch protocol. No claim of complete parity, premium design or 100/100 is made.

The integrated commit must pass clean-install, secret scanning, typecheck,
build and two complete test runs before production publication. Its deployed
frontend, Edge Function and Railway worker must be verified separately.
