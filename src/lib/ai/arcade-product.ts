import type { DesignTokens } from "../projects/design-tokens.ts";
import { accentButtonPair, contrastRatio } from "../projects/visual-quality.ts";

const icons = {
  scopri: '<path d="m12 3 3 6 6 3-6 3-3 6-3-6-6-3 6-3z"/>',
  gioca: '<rect x="3" y="6" width="18" height="13" rx="5"/><path d="M6 12h6m-3-3v6m7-4h.1m2 3h.1"/>',
  preferiti: '<path d="m12 3 2.8 5.7 6.2.9-4.5 4.4 1.1 6.2-5.6-3-5.6 3 1.1-6.2L3 9.6l6.2-.9z"/>',
  record: '<path d="M8 3h8v5a4 4 0 0 1-8 0zm4 9v6m-4 3h8M8 5H4v3a4 4 0 0 0 4 4m8-7h4v3a4 4 0 0 1-4 4"/>',
  impostazioni: '<circle cx="12" cy="8" r="4"/><path d="M4 21v-2a8 8 0 0 1 16 0v2"/>',
};
function icon(id: keyof typeof icons) {
  return `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[id]}</svg>`;
}

/** Honest working starting product for broad playable-arcade briefs, not a catalogue. */
export function arcadeProductHtml(tokens: DesignTokens): string {
  const p = tokens.palette;
  const button = accentButtonPair(p.accent);
  const tabs = Object.entries({ scopri: "Scopri", gioca: "Gioca", preferiti: "Preferiti", record: "Record", impostazioni: "Profilo" });
  return `<!doctype html><html lang="it" data-grammar="arcade" data-fenix-managed-ui="1"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Arcade</title>
<style data-fenix-craft data-fenix-phone>
:root{--bg:${p.bg};--surface:${p.surface};--fg:${p.fg};--muted:${p.fg};--accent:${button.bg};--action:${button.bg};--nav-active:${contrastRatio(button.bg,p.surface)>=4.5 ? button.bg : p.fg};--accent-ink:${button.ink};--line:${p.line}}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--fg);font:16px/1.5 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input{font:inherit}button{cursor:pointer;min-height:44px;border:0;border-radius:16px;padding:12px 18px;background:var(--surface);color:var(--fg)}button:disabled{opacity:.6;cursor:wait}button:focus-visible,input:focus-visible{outline:3px solid var(--action);outline-offset:3px}svg{width:24px;height:24px;flex-shrink:0}header{display:flex;align-items:center;gap:12px;padding:24px 22px 12px}header .mark{display:grid;place-items:center;width:46px;height:46px;border-radius:14px;background:var(--action);color:var(--accent-ink)}header h1{font-size:24px;letter-spacing:-.8px;margin:0}header p{margin:0;font-size:13px}main{max-width:760px;margin:auto;padding:12px 22px 110px;min-height:75vh}h2{font-size:30px;line-height:1.15;letter-spacing:-1px;margin:12px 0}h3{margin:0;font-size:20px}p{margin:10px 0}.eyebrow{font-size:12px;letter-spacing:1.4px;text-transform:uppercase}.hero{position:relative;overflow:hidden;border-radius:28px;background:#172238;color:#fff;padding:28px;margin-bottom:28px}.hero p{color:#e2e8f0;max-width:30ch}.hero svg{width:90px;height:90px;display:block;margin:12px 0;color:#b4f5df}.primary{background:var(--action);color:var(--accent-ink);font-weight:650}.hero .primary{background:#b4f5df;color:#14283c}.games{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.game{border-radius:22px;background:var(--surface);padding:18px;min-width:0}.game p{font-size:14px}.tile-art{display:flex;align-items:center;justify-content:center;height:92px;border-radius:16px;background:#e0e7ff;color:#273b77;margin-bottom:16px;font-size:38px;font-weight:700}.game:nth-child(2) .tile-art{background:#d5f5e9;color:#20513f}.game button{width:100%;margin-top:8px}.sub{font-size:14px}.panel{background:var(--surface);padding:22px;border-radius:24px;margin-top:18px}.board{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin:20px 0}.board button{aspect-ratio:1;padding:0;font-size:26px;background:var(--bg);border:1px solid var(--line)}.board button[aria-pressed="true"]{background:var(--action);color:var(--accent-ink)}#reflex{width:100%;min-height:170px;margin:20px 0;background:#273b77;color:#fff}#reflex.ready{background:#b4f5df;color:#14283c}label{display:block;margin:16px 0 6px}input{width:100%;min-height:48px;border:1px solid var(--line);border-radius:12px;background:var(--bg);color:var(--fg);padding:10px}nav{position:fixed;bottom:0;left:0;right:0;display:flex;background:var(--surface);border-top:1px solid var(--line);padding:8px 6px max(8px,env(safe-area-inset-bottom));z-index:2}nav button{flex:1;min-width:0;display:flex;flex-direction:column;align-items:center;gap:4px;padding:6px 0;font-size:11px;border-radius:12px}nav button[aria-current="page"]{color:var(--nav-active);font-weight:700}#notice{padding:0 22px;color:var(--fg)}[hidden]{display:none!important}.row{display:flex;justify-content:space-between;gap:12px;padding:16px 0;border-bottom:1px solid var(--line)}@media(max-width:350px){main{padding:12px 14px 110px}.games{grid-template-columns:1fr}.hero{padding:22px}}@media(prefers-reduced-motion:no-preference){button{transition:background .15s}}
</style></head><body>
<header><span class="mark" data-fenix-id="icon:app">${icon("gioca")}</span><div><h1>Arcade</h1><p>Piccole sfide. Veri record.</p></div></header><p id="notice" role="status" aria-live="polite"></p>
<main id="root">
<section data-screen="scopri"><div class="hero"><span class="eyebrow">Il tuo prossimo record</span>${icon("gioca")}<h2>Una pausa.<br>Una nuova sfida.</h2><p>Allena memoria e riflessi con due giochi, subito pronti.</p><button class="primary" data-game="memory">Gioca a Coppie</button></div><h2>Scegli la sfida</h2><div class="games" id="catalog"></div></section>
<section data-screen="gioca" hidden><p class="eyebrow">Sala giochi</p><h2 id="game-title">Coppie</h2><p id="instructions">Trova le quattro coppie con meno mosse possibili.</p><div class="panel"><button class="primary" id="start">Inizia partita</button><p id="result" role="status" aria-live="polite">Nessuna partita in corso.</p><div id="board" class="board"></div><button id="reflex" hidden>Aspetta il verde</button></div></section>
<section data-screen="preferiti" hidden><p class="eyebrow">La tua selezione</p><h2>Preferiti</h2><div id="favorites" class="games"></div><p id="empty-favorites">Non hai ancora preferiti. Scegli una sfida da Scopri.</p></section>
<section data-screen="record" hidden><p class="eyebrow">Solo risultati reali</p><h2>I tuoi record</h2><div id="records" class="panel"></div></section>
<section data-screen="impostazioni" hidden><p class="eyebrow">Il tuo spazio</p><h2>Profilo locale</h2><form id="profile" class="panel"><label for="nickname">Nome giocatore</label><input id="nickname" maxlength="32" autocomplete="nickname"><button class="primary" type="submit">Salva nome</button></form><p class="sub">Preferiti e record sono conservati nello spazio dati dell’app. Nessun account o classifica online.</p></section>
</main><nav id="tabs" aria-label="Navigazione principale">${tabs.map(([id,label])=>`<button data-view="${id}"${id === "scopri" ? ' aria-current="page"' : ''}>${icon(id as keyof typeof icons)}<span>${label}</span></button>`).join("")}</nav>
<script>
var COL='arcade',state={favorites:[],records:{},nickname:''},busy=false,game='memory',cards=[],open=[],found=[],moves=0,phase='idle',timer=null,started=0;
var games=[{id:'memory',name:'Coppie',desc:'Quattro coppie. Quanto ricordi?',art:'◈'},{id:'reflex',name:'Riflessi',desc:'Aspetta il verde. Poi tocca.',art:'↯'}];
function el(id){return document.getElementById(id)}
function say(message){el('notice').textContent=message}
async function persist(next){if(busy)return false;busy=true;say('Salvataggio…');try{if(!window.Fenix||!Fenix.save)throw Error('Dati non disponibili');var r=await Fenix.save(COL,next);if(!r||r.ok!==true)throw Error('Salvataggio non confermato');state=next;say('Salvato');render();return true}catch(e){say('Non salvato. Riprova: i dati precedenti sono intatti.');return false}finally{busy=false}}
function card(g){var node=document.createElement('article');node.className='game';node.innerHTML='<div class="tile-art" aria-hidden="true">'+g.art+'</div><h3>'+g.name+'</h3><p>'+g.desc+'</p><button class="primary" data-game="'+g.id+'">Gioca a '+g.name+'</button><button data-favorite="'+g.id+'" aria-pressed="'+state.favorites.includes(g.id)+'">'+(state.favorites.includes(g.id)?'Rimuovi preferito':'Salva preferito')+'</button>';return node}
/*fenix-slot:render*/function render(){el('catalog').replaceChildren();el('favorites').replaceChildren();games.forEach(function(g){el('catalog').append(card(g));if(state.favorites.includes(g.id))el('favorites').append(card(g))});el('empty-favorites').hidden=state.favorites.length>0;el('records').replaceChildren();games.forEach(function(g){var row=document.createElement('div');row.className='row';var label=document.createElement('strong');label.textContent=g.name;var value=document.createElement('span');value.textContent=Number.isFinite(state.records[g.id])?state.records[g.id]+(g.id==='memory'?' mosse':' ms'):'Ancora da conquistare';row.append(label,value);el('records').append(row)});el('nickname').value=state.nickname}
function cancel(){clearTimeout(timer);timer=null;phase='idle';open=[];el('board').replaceChildren();el('reflex').hidden=true;el('start').hidden=false;el('result').textContent='Nessuna partita in corso.'}
function view(id){cancel();document.querySelectorAll('[data-screen]').forEach(function(s){s.hidden=s.dataset.screen!==id});document.querySelectorAll('nav button').forEach(function(b){if(b.dataset.view===id)b.setAttribute('aria-current','page');else b.removeAttribute('aria-current')})}
function choose(id){if(!games.some(function(g){return g.id===id}))return;game=id;view('gioca');el('game-title').textContent=id==='memory'?'Coppie':'Riflessi';el('instructions').textContent=id==='memory'?'Trova le quattro coppie con meno mosse possibili.':'Tocca solo quando il riquadro diventa verde.'}
async function finish(score){phase='done';el('start').hidden=false;el('start').textContent='Rigioca';el('result').textContent='Partita conclusa: '+score+(game==='memory'?' mosse.':' ms.');if(!Number.isFinite(state.records[game])||score<state.records[game]){var records=Object.assign({},state.records);records[game]=score;await persist(Object.assign({},state,{records:records}))}}
function paint(){el('board').replaceChildren();cards.forEach(function(value,index){var b=document.createElement('button');var shown=open.includes(index)||found.includes(index);b.textContent=shown?value:'?';b.setAttribute('aria-label',shown?'Carta '+value:'Scopri carta '+(index+1));b.setAttribute('aria-pressed',String(shown));b.disabled=found.includes(index);b.dataset.card=String(index);el('board').append(b)})}
el('start').onclick=function(){cancel();phase='playing';el('start').hidden=true;el('result').textContent='Partita in corso';if(game==='memory'){cards=['◆','●','▲','✦','◆','●','▲','✦'];for(var i=cards.length-1;i>0;i--){var j=Math.floor(Math.random()*(i+1));var t=cards[i];cards[i]=cards[j];cards[j]=t}found=[];moves=0;paint()}else{phase='waiting';el('reflex').hidden=false;el('reflex').className='';el('reflex').textContent='Aspetta il verde';timer=setTimeout(function(){phase='ready';started=performance.now();el('reflex').className='ready';el('reflex').textContent='Ora! Tocca'},1000+Math.random()*2000)}};
el('reflex').onclick=function(){if(phase==='waiting'){cancel();el('result').textContent='Troppo presto. Riprova.'}else if(phase==='ready'){el('reflex').hidden=true;finish(Math.max(1,Math.round(performance.now()-started)))}};
el('board').onclick=function(e){var b=e.target.closest('[data-card]');if(!b||phase!=='playing'||open.length===2)return;var i=Number(b.dataset.card);if(open.includes(i)||found.includes(i))return;open.push(i);paint();if(open.length===2){moves++;if(cards[open[0]]===cards[open[1]]){found.push(open[0],open[1]);open=[];paint();if(found.length===cards.length)finish(moves)}else timer=setTimeout(function(){open=[];paint()},650)}};
document.addEventListener('click',function(e){var b=e.target.closest('button');if(!b)return;if(b.dataset.view)view(b.dataset.view);if(b.dataset.game)choose(b.dataset.game);if(b.dataset.favorite){var id=b.dataset.favorite;var f=state.favorites.includes(id)?state.favorites.filter(function(x){return x!==id}):state.favorites.concat(id);persist(Object.assign({},state,{favorites:f}))}});
/*fenix-slot:save*/function save(){return persist(Object.assign({},state,{nickname:el('nickname').value.trim().slice(0,32)}))}
el('profile').onsubmit=function(e){e.preventDefault();save()};
async function boot(){try{var s=window.Fenix&&Fenix.load?await Fenix.load(COL):null;if(s&&typeof s==='object'){state.favorites=Array.isArray(s.favorites)?s.favorites.filter(function(v){return v==='memory'||v==='reflex'}):[];state.records={};['memory','reflex'].forEach(function(k){if(s.records&&Number.isFinite(s.records[k])&&s.records[k]>0)state.records[k]=s.records[k]});state.nickname=typeof s.nickname==='string'?s.nickname.slice(0,32):''}}catch(e){say('Caricamento dati non riuscito.')}render();document.documentElement.setAttribute('data-fenix-ready','true')};boot();
</script></body></html>`;
}
