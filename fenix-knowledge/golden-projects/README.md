# Golden projects

Empty in Slice 1.

**Future:** ingest 10 Emergent export zips. For each zip, extract reusable patterns into `architecture`, `ui-patterns`, `auth`, `database`, `api`, `ai-features`, `marketplace`, `admin`, and `mobile`. Store provenance here — never vendor the cloned tree as a compose seed.
